<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="?android:selectableItemBackground"
    android:layout_marginBottom="8dp">

    <ImageView
        android:id="@+id/imageSearchMoviePoster"
        android:layout_width="80dp"
        android:layout_height="120dp"
        android:scaleType="centerCrop"
        android:contentDescription="Movie Poster"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/textSearchMovieTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Movie Title"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="16sp"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginStart="12dp"
        app:layout_constraintStart_toEndOf="@id/imageSearchMoviePoster"
        app:layout_constraintTop_toTopOf="@id/imageSearchMoviePoster"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/textSearchMovieYear"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="2024"
        android:textColor="#CCCCCC"
        android:textSize="14sp"
        app:layout_constraintStart_toStartOf="@id/textSearchMovieTitle"
        app:layout_constraintTop_toBottomOf="@id/textSearchMovieTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>
